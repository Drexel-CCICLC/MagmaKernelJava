function Array_size(size) {
    return new Array(size);
}

function Array_length(array) {
    return array.length;
}

function Array_get(array, index) {
    return array[index];
}

function Array_set(array, index, value) {
    array[index] = value;
}