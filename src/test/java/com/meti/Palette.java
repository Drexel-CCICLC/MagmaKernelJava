package com.meti;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class Palette {
	@Test
	void describe() {
		assertEquals("10", new Compiler().compile());
	}
}
